<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- ছোট widget: আজকের তারিখ (ছবি ১ এর মতো) -->
        <receiver
            android:name=".DailyWidgetProvider"
            android:exported="false"
            android:label="@string/daily_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_daily_info" />
        </receiver>

        <!-- বড় widget: পুরো মাসের ক্যালেন্ডার (ছবি ২ এর মতো) -->
        <receiver
            android:name=".MonthlyWidgetProvider"
            android:exported="false"
            android:label="@string/monthly_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.bnwidget.calendar.ACTION_PREV_MONTH" />
                <action android:name="com.bnwidget.calendar.ACTION_NEXT_MONTH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_monthly_info" />
        </receiver>

    </application>
</manifest>
